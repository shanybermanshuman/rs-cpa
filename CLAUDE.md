@AGENTS.md

# מערכת ניהול לקוחות ומשימות – סרנגה ושומן רואות חשבון

מערכת ווב פנימית למשרד רואות חשבון: ניהול לקוחות, מעקב אחר משימות סטטוטוריות
חוזרות (מע"מ, ביטוח לאומי, מקדמות, דוחות שנתיים) ומשימות פנימיות של המשרד.
תכנית העבודה המאושרת: `C:\Users\User\.claude\plans\vast-leaping-crayon.md`.

## דרישות מוצר מחייבות

- **כל הממשק בעברית ובכיוון RTL.** לא תרגום מאוחר — כל מסך/טופס/הודעה נבנים כך מלכתחילה.
  `<html lang="he" dir="rtl">` מוגדר ב-`src/app/layout.tsx`.
- **מיתוג RS:** שחור (`#111111`) וזהב (`#C9A456`, נדגם מקובץ הלוגו) על רקע לבן/קרם.
  הצבעים מוגדרים כמשתני CSS ב-`src/app/globals.css`.
- **הלוגו:** קובץ המקור הוא `לוגו.jpg`. הנכסים נבנים ממנו בפקודה `npm run build:logo`
  (`scripts/build-logo.ts`, מבוסס `sharp`) שחותכת את השוליים הלבנים ומייצרת את
  `public/logo.png` ו-`public/monogram.png`. את המונוגרמה יש להעתיק ידנית ל-`src/app/icon.png`
  כדי שתשמש כאייקון הדפדפן.
- הרקע של הלוגו לבן במכוון, ולכן **הכותרת של המערכת לבנה עם קו זהב תחתון** —
  בהתאם לניירת. אין להציב את הלוגו על רקע כהה.
- **אין להמיר את `לוגו.pdf` ל-SVG בכתיבת parser ידני** — זה נוסה והתוצאה הייתה משובשת.
- **חובה לאמת שינויי לוגו/עיצוב ויזואלית** (לקרוא את קובץ התמונה עצמו, או לבקש
  מהמשתמש לאשר) ולא להסתפק במדידות גאומטריה.
- **התראות: מומשו לפי בקשה מפורשת של הלקוח (אוגוסט 2026).** ראו "מנגנון ההתראות" למטה.
  מיילים נשלחים **לצוות המשרד בלבד ולעולם לא ללקוחות** — זו החלטה מפורשת של הלקוח.
  שליחת מייל ללקוח דורשת בקשה מפורשת חדשה, ניסוח מאושר ובקרה ידנית לפני שליחה.
- **הדוח השנתי מנוהל לפי גלי אורכות** (`AnnualExtension`) ולא לפי מועד אחד: המשרד
  מקבל מרשות המסים כמה מועדי הגשה לאותה שנת מס. שיוך לקוח למועד מעדכן גם את
  `dueDate` של המשימה, כדי שחישובי הדחיפות יסתמכו על שדה אחד.
- **שני מסלולים נפרדים לדוח השנתי:** דוח מבוקר של חברה מול דוח אישי של יחיד.
  לכל מסלול מועדי אורכה משלו (`AnnualExtension.kind`), ומסך הדוחות מפוצל
  ללשוניות. הסוג **נגזר מסוג הלקוח** (`annualReportKind` ב-`src/lib/annual.ts`):
  חברה ← מבוקר, עצמאי ובעל שליטה ← אישי. אין לאחד את המסלולים.
- **השיוך למועד אורכה הוא ידני לכל לקוח — אין כלל קבוע.** נקבע במפורש ע"י הלקוח.
  אין לממש שיוך אוטומטי לפי סוג לקוח, ותק או כל היוריסטיקה אחרת. השיוך הקבוצתי
  הקיים ("שיוך כל מי שטרם שויך") הוא קיצור דרך יזום בלבד.

## סטאק

Next.js 16 (App Router) · TypeScript · Tailwind v4 · shadcn/ui · Prisma 7 · Supabase (Postgres + Auth) · פריסה מיועדת ל-Vercel.

## מלכודות סביבה — לקרוא לפני הרצת פקודות

### Node לא נמצא ב-PATH
Node מותקן ב-`C:\Program Files\nodejs` אך **אינו** ב-PATH של shell חדש.
יש לקדם כל פקודה ב-PowerShell ב:
```
$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")
```
שרת הפיתוח מופעל דרך `dev-server.cmd` (שמסדר PATH בעצמו), כמוגדר ב-`.claude/launch.json`.

### שני חיבורים שונים למסד הנתונים — אל תאחדו אותם
Supabase מספק שני poolers, ולכל אחד תפקיד אחר כאן:

| משתנה | פורט | למי |
|---|---|---|
| `DATABASE_URL` | 6543 (Transaction Pooler) | האפליקציה בזמן ריצה (`src/lib/prisma.ts`) |
| `DIRECT_URL` | 5432 (Session Pooler) | פקודות ה-CLI של Prisma (`prisma.config.ts`) |

- **מיגרציות דרך פורט 6543 נתקעות ללא הודעת שגיאה.** לכן `prisma.config.ts` מפנה
  את ה-CLI ל-`DIRECT_URL`. אין להחזיר אותו ל-`DATABASE_URL`.
- החיבור הישיר הקלאסי (`db.<ref>.supabase.co:5432`) **אינו נגיש** מהמחשב הזה
  (Supabase מספק אותו ב-IPv6 בלבד) — יש להשתמש ב-Session Pooler.
- `directUrl` בתוך `datasource` ב-`prisma.config.ts` **אינו נתמך** בגרסה זו ומתעלמים ממנו בשקט.

### מיגרציות: לא להשתמש ב-`prisma migrate dev`
`migrate dev` מנסה ליצור shadow database ואין לו הרשאה לכך ב-Supabase — הפקודה נתקעת.
התהליך הנכון ליצירת מיגרציה חדשה:
```bash
npx prisma migrate diff --from-config-datasource --to-schema prisma/schema.prisma --script --output prisma/migrations/<n>_<name>/migration.sql
npx prisma migrate deploy
npx prisma generate
```
משווים מול **מסד הנתונים החי** (`--from-config-datasource`) ולא מול תיקיית המיגרציות
(`--from-migrations`), משום שהאחרון דורש shadow database שאין לו הרשאה ב-Supabase.

### אחרי `prisma generate` — חובה להפעיל מחדש את שרת הפיתוח
שרת הפיתוח מחזיק בזיכרון את לקוח ה-Prisma שנטען בעת ההפעלה. אחרי הוספת שדה
או טבלה, השרת הרץ לא יכיר אותם והשגיאות מטעות: שדות חדשים חוזרים כ-`undefined`
(ומופיעים כ-`NaN` במסך), ו-`include` על קשר חדש נכשל ב-
`Unknown field ... for include statement`. **הפתרון תמיד: להפעיל מחדש את השרת.**

### חיפוש חופשי — חובה לעבור דרך `src/lib/search.ts`
`contains` של Prisma מעביר את הערך כפי שהוא ל-LIKE ו**אינו** מנטרל `%` ו-`_`,
ולכן חיפוש של `%` החזיר את כל הלקוחות. כל שדה חיפוש חופשי חייב לעבור דרך
`escapeLike`. חיפוש טלפון חייב לעבור דרך `phoneSearchDigits` **בשני צדי
ההשוואה** — הטלפון נשמר כפי שהוקלד, ואותו מספר יכול להיות שמור כ-`052-1234567`,
`052 123 4567` או `+972-52-1234567`. הנרמול בצד ה-SQL חייב להישאר זהה לזה שב-JS.
`/clients` הוא כרגע החיפוש החופשי היחיד במערכת.

### שדות טופס לא-חובה — חובה `.optional()` ב-Zod
שדה שאינו קיים בטופס, או תיבת סימון שאינה מסומנת, פשוט אינו נשלח — והמפתח
חסר לגמרי מה-FormData. Zod דוחה מפתח חסר **לפני** בדיקת הערך, והתוצאה היא
כישלון שמירה עם שגיאה כללית בלי שאף שדה מסומן. הבאג הזה כבר קרה כאן פעמיים.
יש להשתמש תמיד בעזרים מ-`src/lib/form-schema.ts` (`optionalText`, `checkbox`,
`optionalDate`, `optionalNumber`) ולא להגדיר שדות אופציונליים ידנית.

### קובץ `"use client"` — אין לייבא ממנו פונקציית עזר לקוד שרת
בשכבת ה-RSC כל ייצוא מקובץ `"use client"` מוחלף בהפניה ללקוח, ולכן **קריאה**
לפונקציית עזר משם בתוך רכיב שרת נכשלת בזמן ריצה:
`Attempted to call formatAmount() from the server but formatAmount is on the client`.
TypeScript אינו תופס זאת ו-`npm run typecheck` עובר. גרוע מכך, אם כל קריאה
מוגנת בבדיקת `null` הבאג נשאר רדום עד שיוזן הנתון הראשון.
פונקציות עזר טהורות שייכות ל-`src/lib/` (למשל `src/lib/format.ts`) — בדיוק
כמו הכלל המקביל בקבצי `"use server"` שמתחתיו.

### קובץ `"use server"` — כל ייצוא חייב להיות async
Next.js דוחה כל ייצוא סינכרוני מקובץ פעולות שרת, גם אם הוא פונקציית עזר טהורה.
TypeScript אינו תופס זאת. פונקציות עזר יש למקם ב-`src/lib/` (למשל `src/lib/annual.ts`).

### Prisma 7 דורש driver adapter
אין `datasourceUrl` בבנאי של `PrismaClient`; יש להעביר `adapter` (`@prisma/adapter-pg`).
כמו כן הייבוא הוא `@/generated/prisma/client` — **לא** `@/generated/prisma` (אין קובץ index).

## מנוע המשימות החוזרות (הליבה)

- `src/lib/recurrence.ts` — הלוגיקה הטהורה: מתי חל מועד ההגשה ואיזו תקופה הוא מכסה.
  **מכוסה בבדיקות** (`src/lib/recurrence.test.ts`) — יש להריץ `npm test` אחרי כל שינוי כאן.
- `src/lib/task-generator.ts` — יצירת המשימות בפועל מול מסד הנתונים, וסימון משימות באיחור.
- `src/app/api/cron/generate-tasks/route.ts` — ריצה יומית (04:00) המוגדרת ב-`vercel.json`,
  מאובטחת ב-`CRON_SECRET`.

עיקרון מנחה: הדיווח מוגש **אחרי** תום תקופת הדיווח (מע"מ לינואר מוגש ב-15 בפברואר).
לכן מע"מ דו-חודשי מוגש בחודשים האי-זוגיים בלבד.

טווח היצירה: 3 חודשים קדימה לדיווחים השוטפים, אך **14 חודשים לדוח השנתי** —
העבודה עליו נמשכת חודשים ומתחילה הרבה לפני מועד ההגשה באפריל.

## הפרדה בין דיווחים חוזרים למשימות — קריטי להבנה

**דיווח חוזר אינו "משימה" של הלקוח** אלא חובה מתמשכת שחוזרת כל חודש.
הצגתו כמשימה יוצרת מאות שורות חוזרות ומסתירה את מה שבאמת דורש טיפול.
זו הייתה בעיה אמיתית שדווחה ע"י הלקוח, ולכן ההפרדה הבאה נבחרה במכוון:

| מסך | תוכן |
|---|---|
| `/filings` | **לוח מעקב הדיווחים החוזרים.** לקוחות מול תקופות, לחיצה על תא = "הוגש" |
| `/tasks` | משימות **חד-פעמיות** בלבד (`recurrenceRuleId = null`) |
| `/annual-reports` | הדוח השנתי, לפי שנת מס וגלי אורכות |
| `/tasks/all` | רשימה שטוחה — תצוגה משנית לחיפוש נקודתי בלבד |

- הלוח (`src/lib/filing-board.ts`) בשלוש תצוגות:
  **לפי סוג דיווח** (שורות = לקוחות, עמודות = 12 חודשי התקופה, כך שלקוח חודשי
  ודו-חודשי יושבים באותה טבלה), **לפי חודש** (עמודות = סוגי דיווח),
  ו**לפי לקוח** (שורות = סוגי הדיווח של אותו לקוח על פני השנה).
- **בכרטיס הלקוח** הדיווחים החוזרים מוצגים כשורת סיכום אחת לכל סוג
  ("מסודר עד יולי 2026") ולא כרשימת משימות חודשית.

### סכומי דיווח ובדיקת סבירות

`ClientTask.amount` שומר את סכום הדיווח, כדי לאפשר השוואה בין תקופות.
`amountStats` ב-`src/lib/filing-board.ts` בונה סדרה לכל צירוף (לקוח × סוג דיווח)
לפי סדר מועדי ההגשה, ולכל סכום מחשב שינוי מול הקודם וסטייה מממוצע **שלוש**
התקופות הקודמות. סטייה של 50% ומעלה מסומנת כחריגה.

- ההשוואה היא מול ממוצע ולא מול התקופה הקודמת בלבד — בדיווחי מע"מ יש תנודתיות
  טבעית, וחודש נמוך אחד היה מייצר התרעת שווא בכל חודש שאחריו.
- **שדה ריק מנקה את הסכום (`null`) ואינו נשמר כאפס.** "לא הוזן" ו"דיווח אפס"
  הם מצבים שונים, ואיחודם היה משבש את הממוצע.
- השאילתות מושכות תקופות אחורה מעבר לשנה המוצגת (`dueRangeForPeriodYear`
  עם `lookbackMonths`), אחרת לדיווח של ינואר לא היה מול מה להשוות.

**אין להחזיר דיווחים חוזרים לרשימות המשימות** ואין לבנות רשימה שטוחה כמסך ראשי.

מניעת כפילויות מובטחת ע"י אינדקס ייחודי `@@unique([recurrenceRuleId, dueDate])` בסכמה —
ולכן אפשר להריץ את ה-cron שוב ושוב בבטחה.

## מנגנון ההתראות

`src/lib/alerts.ts` הוא המקור היחיד לכל ההתראות — גם לפעמון במסך וגם למייל היומי.

**ההתראות נגזרות מהנתונים ואינן נשמרות בטבלה.** אין "סימון כנקרא" ואי אפשר
לסגור התראה: היא נעלמת כשהמשימה מוגשת או כשהאישור מחודש. במערכת ציות, התראה
שאפשר לסגור היא התראה שתיסגר ותישכח. **אין להוסיף כפתור סגירה.**

| מקור | חומרה | הערה |
|---|---|---|
| משימה חד-פעמית / דוח שנתי באיחור | קריטי | התראה נפרדת לכל משימה |
| משימה שמועדה בעוד עד 5 ימים | אזהרה | `TASK_LEAD_DAYS` |
| דיווחים שוטפים באיחור | קריטי | **מקובצים להתראה אחת** שמובילה ל-`/filings` |
| אישור ניכוי במקור שפג או פג היום | קריטי | היום האחרון לפעולה הוא עדיין דחוף |
| אישור שעומד לפוג (45 יום) | אזהרה | `WITHHOLDING_WARNING_DAYS` |
| משימה פנימית באיחור | קריטי | |

- **אין התראה מקדימה על דיווח חוזר.** התראה על כל דיווח 5 ימים לפני ה-15 הייתה
  מייצרת עשרות התראות בכל חודש — בדיוק ההצפה שהלקוח ביקש למנוע. לכן דיווחים
  חוזרים מתריעים רק אחרי שהמועד חלף, ובשורה מקובצת אחת.
- ההודעה הקופצת (sonner) מוצגת **פעם אחת לכניסה** דרך `sessionStorage`, ולא בכל
  מעבר בין מסכים.
- ההחרגה היא `status: { not: "INACTIVE" }` ולא `status: "ACTIVE"` — גם לקוח
  בקליטה יכול להיות עם מועד הגשה אמיתי, ושתיקה לגביו היא בדיוק הכישלון
  שההתראות אמורות למנוע.
- `AppShell` הפך ל-`async` כדי לטעון את ההתראות — כל מסך משתמש בו.

### מייל יומי
`src/app/api/cron/daily-digest/route.ts` (05:00 UTC ב-`vercel.json`, אחרי
`generate-tasks` כדי שסימון האיחורים כבר יתבצע). `src/lib/email.ts` שולח דרך
Resend ב-`fetch` ישיר, בלי חבילה נוספת.

- **בלי `RESEND_API_KEY` ו-`ALERT_EMAIL_FROM` המערכת עובדת רגיל** והמסלול מדווח
  שהמייל אינו מוגדר. אין להפיל את הבנייה או את הריצה בגלל מייל.
- **כשאין התראות לא נשלח מייל.** מייל יומי שרובו "אין חדש" מאבד את תשומת הלב
  ביום שבו יש בו משהו.
- ה-cron רץ פעם ביום בלבד ב-Vercel Hobby. "התראה מיידית" אמיתית דורשת תוכנית
  בתשלום, ולכן היא לא מומשה.

## פריסה

המערכת בייצור ב-**https://rs-cpa.vercel.app** (Vercel Hobby, ריפו פרטי
`shanybermanshuman/rs-cpa`). דחיפה ל-`main` בונה ופורסת אוטומטית.

- `npm run build` מריץ `prisma generate` לפני `next build` — לקוח Prisma נוצר
  לתיקייה שאינה ב-git, ובלעדיו הבנייה בענן נכשלת.
- משתני הסביבה מוגדרים ב-Vercel תחת Production. שינוי משתנה דורש **Redeploy**;
  הערכים נאפים לתוך הגרסה הרצה.
- שתי המשימות המתוזמנות רשומות מ-`vercel.json`. בתוכנית Hobby יש **חלון גמישות
  של שעה**, ולכן הסדר ביניהן אינו מובטח. זה לא מזיק: ההתראות נגזרות מ-`dueDate`
  ואינן תלויות בכך ש-`markOverdueTasks` כבר רץ.
- **החלפת סיסמת מסד הנתונים:** אחרי איפוס בסופבייס לוקח כדקה עד ששרתי החיבור
  מקבלים אותה. כישלון `28P01` מיד אחרי האיפוס אינו בהכרח טעות — יש לנסות שוב
  אחרי המתנה קצרה.

### פתוח
- **מפתח `service_role` טרם הוחלף** אף שעבר בצ'אט בשלב ההקמה. החלפתו בסופבייס
  משנה גם את המפתח הציבורי, כלומר שני משתנים לעדכן ב-Vercel וגם מקומית.
- **`RESEND_API_KEY` ו-`ALERT_EMAIL_FROM` אינם מוגדרים**, ולכן הסיכום היומי רץ
  כל בוקר ומסיים בשקט בלי לשלוח. הקוד מוכן ומחכה למפתח בלבד.

## ייבוא לקוחות מאקסל

`scripts/import-clients.ts` קורא את גיליון הלקוחות של המשרד (כותרות בשורה 2, מעמודה B).
**ברירת המחדל היא הרצה יבשה** — הכתיבה בפועל דורשת `--commit`.

```bash
npm run import:clients -- --file "רשימת לקוחות.xlsx"            # תצוגה מקדימה
npm run import:clients -- --file "רשימת לקוחות.xlsx" --commit   # ייבוא בפועל
```

מיפוי הערכים בעברית מוגדר ב-`CLIENT_TYPE_MAP` ו-`SERVICE_TYPE_MAP` בראש הסקריפט.
עמודת **תדירות מע"מ אינה קיימת בגיליון** ולכן נשארת ריקה; משהיא מושלמת בכרטיס
הלקוח, `syncDefaultRulesForClient` מוסיף את כלל המע"מ החסר בעדכון הבא.

## פקודות שימושיות

```bash
npm test                   # בדיקות מנוע המשימות
npm run typecheck          # בדיקת טיפוסים
npx prisma generate        # אחרי כל שינוי בסכמה
npx prisma migrate deploy  # החלת מיגרציות
npx prisma migrate status  # בדיקת מצב
```

בדיקת תקינות חיבור מקצה לקצה (ציבורית, ללא התחברות):
```bash
curl.exe -s http://localhost:3000/api/health
```
מחזירה `{"status":"ok","database":"connected"}` כשהכל תקין.

## מבנה

- `prisma/schema.prisma` — מודל הנתונים. ה-enums מתועדים בעברית בהערות.
- `src/lib/prisma.ts` — לקוח Prisma יחיד (singleton) לזמן ריצה.
- `src/lib/supabase/` — לקוחות Supabase ל-client/server/middleware.
- `src/middleware.ts` — הגנת מסלולים; מפנה משתמש לא מחובר ל-`/login`.
  `/api/health` פתוח בכוונה לניטור חיצוני.
- `src/generated/prisma/` — נוצר אוטומטית, אינו ב-git.

## סודות

`.env` מכיל מפתחות אמיתיים של Supabase ואינו ב-git (ראו `.env.example`).
`SUPABASE_SERVICE_ROLE_KEY` הוא סוד — לשימוש בקוד שרת בלבד, לעולם לא ב-client.
